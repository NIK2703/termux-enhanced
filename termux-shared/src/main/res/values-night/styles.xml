<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Night variant of BaseActivity.Scheme.PopupMenu.ItemText (see values/styles.xml):
         resolve through ?android:attr/textColorPrimary so the item text always matches the
         popup background, which is also resolved from the theme (?attr/colorSurface). -->
    <style name="BaseActivity.Scheme.PopupMenu.ItemText" parent="TextAppearance.MaterialComponents.Subtitle1">
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>

</resources>
