<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        layout="@layout/partial_primary_toolbar"
        android:id="@+id/partial_primary_toolbar"/>

    <!-- Custom action buttons contributed by the host app (see ReportActivity.ReportActionHost,
         e.g. the container/settings backups on the crash report screen) are NOT a separate view
         here: they are the first items of the report list below, so they sit at the very top of the
         report and scroll away with it instead of staying pinned under the toolbar. -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:overScrollMode="never"
        android:paddingTop="@dimen/content_padding"
        android:paddingBottom="36dip" />

</LinearLayout>
