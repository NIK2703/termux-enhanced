<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/session_tab_root"
    android:layout_width="wrap_content"
    android:layout_height="@dimen/terminal_tab_height_double"
    android:layout_marginEnd="4dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/session_tab_background"
    android:paddingStart="12dp"
    android:paddingEnd="4dp">

    <TextView
        android:id="@+id/session_tab_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="@dimen/terminal_tab_title_max_width"
        android:ellipsize="end"
        android:maxLines="2"
        android:textSize="13sp"
        android:textColor="?attr/colorOnSurface" />

    <ImageButton
        android:id="@+id/session_tab_close"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="@dimen/terminal_tab_close_gap"
        android:src="@drawable/ic_close"
        android:background="@null"
        android:contentDescription="@string/tab_close_description"
        android:scaleType="centerInside"
        android:tint="?attr/colorOnSurface"
        android:alpha="0.7"
        android:visibility="invisible"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
